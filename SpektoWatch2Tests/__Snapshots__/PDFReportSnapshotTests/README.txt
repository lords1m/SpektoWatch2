Snapshot baselines for PDFReportSnapshotTests live in this folder.

Do not delete this file — it exists so Xcode treats the parent folder
reference as a non-empty resource and bundles it into the .xctest
product. Once real .png / .txt baselines land here (first Xcode Cloud
run with RECORD_SNAPSHOTS=YES), this README is redundant but harmless.

See SpektoWatch2Tests/SnapshotTestSupport.swift for setup notes.
